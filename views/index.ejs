<% include header.ejs %>

<% if(!user) { %>
  <div class="jumbotron">
      <div class="container">
        <h1>欢迎来到 Microblog</h1>
        <p>Microblog 是一个基于 Node.js 的微博系统。</p>
            <p>
              <a class="btn btn-primary btn-lg" href="/login" role="button">登录 &raquo;</a>
              <a class="btn btn-default btn-lg" href="/reg" role="button">立即注册</a>
            </p>
      </div>
  </div>
<% } %>

<div class="container">
  <% if (user) {%>
    <% include says.ejs %>
  <% } %>

  <% include posts.ejs %>
</div>
<% include footer.ejs %>
